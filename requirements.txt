# Supported runtime: Python 3.10-3.12
#
# Install the published wheel to avoid requiring a local C++ compiler for the
# optional Cython graph extension. The repository's local CHGNet source remains
# available and falls back to its Python graph implementation when necessary.
chgnet==0.4.2

# GATGNN runtime dependencies not provided by CHGNet.
torch==2.12.1
pandas==2.3.3
scikit-learn==1.9.0
torch-geometric==2.8.0
tabulate==0.10.0
